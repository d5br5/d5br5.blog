import { DialogContent } from '@radix-ui/react-dialog';

export default function ProjectModalContent({ children }: { children: React.ReactNode }) {
  return <DialogContent>{children}</DialogContent>;
}
